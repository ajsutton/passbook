/target
/.vendor
# The entire cargo config is machine-local and generated by
# scripts/seed-vendor.sh (it contains an absolute path to the local mirror).
# Nothing under .cargo/ is committed; regenerate per machine / CI / Docker.
/.cargo/
**/*.rs.bk
*.pdb
